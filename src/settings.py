TITLE="example.xyz"
DESCRIPTION="An example image gallery"

# If the whitelist is not empty, only tags defined in it be displayed as website sections.
# Tags defined in the blacklist won't be displayed as website sections. The whitelist takes precedence
# over the blacklist. If both are empty, all tags will be displayed as website sections.
# Example usage: TAGS_WHITELIST = ["cat", "dog"]
TAGS_WHITELIST = []
TAGS_BLACKLIST = []
# By default, tags are ordered alphabetically. Set TAG_CUSTOM_ORDER to True to follow the order in TAGS_WHITELIST.
TAG_CUSTOM_ORDER = False
